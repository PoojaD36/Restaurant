/**
 * DTO for marking an order as ready
 * Empty DTO - no params needed, orderId comes from route parameter
 */
export class MarkOrderReadyDto {
  // No fields needed - orderId comes from route parameter
}
